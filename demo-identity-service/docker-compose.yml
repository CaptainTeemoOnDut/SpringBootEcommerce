
  services:
    redis:
      image: redis:6.2
      ports:
      - "6379:6379"

    mysql:
      image: mysql:8.0
      ports:
        - "3307:3306"
      environment:
        MYSQL_ROOT_PASSWORD: root
        MYSQL_DATABASE: chatdb
      volumes:
        - ./mysql_data:/var/lib/mysql
